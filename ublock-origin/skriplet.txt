ac.js application/javascript
(() => {
    const token = "{{1}}";
    if (token === "" || token === "{{1}}") return;
    const tokens = token.split(/\s*\|\s*/);
    const selector = "{{2}}";
    if (selector === "" || selector === "{{2}}") return;
    const addclass = () => {
        try {
            document.querySelector(selector).classList.add(...tokens);
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", addclass, { capture: true, once: true });
    } else {
        addclass();
    }
})();

ceol.js application/javascript
(() => {
    const selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") return;
    const clicked = () => {
        document.querySelector(selector).click();
    };
    if (document.readyState === "complete") {
        clicked();
    } else {
        window.addEventListener("load", clicked);
    }
})();

cepn.js application/javascript
(() => {
    const path = "{{1}}";
    const lplc = location.pathname.toLowerCase();
    if (path === "" || path === "{{1}}" || lplc.indexOf(path) === -1) return;
    const prev = "{{2}}";
    if (prev === "" || prev === "{{2}}") return;
    const next = "{{3}}";
    if (next === "" || next === "{{3}}") return;
    window.addEventListener("keyup", (ev) => {
        if (ev.altKey && ev.key === "b") {
            document.querySelector(prev).click();
        } else if (ev.altKey && ev.key === "n") {
            document.querySelector(next).click();
        }
    }, false);
})();

css.js application/javascript
(() => {
    const path = "{{1}}";
    const lplc = location.pathname.toLowerCase();
    if (path === "" || path === "{{1}}" || lplc.indexOf(path) === -1) return;
    let selector = "{{2}}";
    if (selector === "" || selector === "{{2}}") return;
    if (selector.indexOf("|") !== -1) selector = selector.split(/\s*\|\s*/).join(",");
    const style = "{{3}}";
    if (style === "" || style === "{{3}}") return;
    const addstyle = () => {
        try {
            const elements = document.querySelectorAll(selector);
            for (const element of elements) {
                element.style.cssText = style;
            }
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", addstyle, { capture: true, once: true });
    } else {
        addstyle();
    }
})();

html.js application/javascript
(() => {
    const selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") return;
    const position = "{{2}}";
    if (position === "" || position === "{{2}}") return;
    const html = "{{3}}";
    if (html === "" || html === "{{3}}") return;
    const inserthtml = () => {
        document.querySelector(selector).insertAdjacentHTML(position, html);
    };
    if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", inserthtml, false);
    } else {
        inserthtml();
    }
})();

olin.js application/javascript
(() => {
    let selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") return;
    if (selector.indexOf("|") !== -1) selector = selector.split(/\s*\|\s*/).join(",");
    const targetblank = () => {
        try {
            const elements = document.querySelectorAll(selector);
            for (const element of elements) {
                if (element.href.indexOf(location.hostname) === -1) {
                    element.setAttribute("target", "_blank");
                }
            }
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", targetblank, { once: true });
    } else {
        targetblank();
    }
})();

sa.js application/javascript
(() => {
    let selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") return;
    if (selector.indexOf("|") !== -1) selector = selector.split(/\s*\|\s*/).join(",");
    const name = "{{2}}";
    if (name === "" || name === "{{2}}") return;
    const value = "{{3}}";
    if (value === "" || value === "{{3}}") return;
    const setattr = () => {
        try {
            const elements = document.querySelectorAll(selector);
            for (const element of elements) {
                element.setAttribute(name, value);
            }
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", setattr, { once: true });
    } else {
        setattr();
    }
})();

sck.js application/javascript
(() => {
    const data = "{{1}}";
    if (data.indexOf("=") === -1) return;
    document.cookie = data;
})();

scke.js application/javascript
(() => {
    let data = "{{1}}";
    const path = "{{2}}";
    const domain = "{{3}}";
    const secure = "{{4}}";
    const del = "{{5}}";
    if (data.indexOf("=") === -1) return;
    if (del === "true") {
        data += ";max-age=-100";
    } else {
        data += ";max-age=2592000";
    }
    if (path !== "" && path !== "{{2}}") {
        data += ";path=" + path;
    }
    if (domain !== "" && domain !== "{{3}}") {
        data += ";domain=" + domain;
    }
    if (secure === "true") {
        data += ";secure";
    }
    document.cookie = data;
})();

wsol.js application/javascript
(() => {
    const path = "{{1}}";
    const lplc = location.pathname.toLowerCase();
    if (path === "" || path === "{{1}}" || lplc.indexOf(path) === -1) return;
    const ytop = "{{2}}";
    if (ytop === "" || ytop === "{{2}}") return;
    const scrolled = () => {
        window.scroll({
            left: 0,
            top: ytop,
            behavior: "smooth",
        });
    };
    if (document.readyState === "complete") {
        scrolled();
    } else {
        window.addEventListener("load", scrolled);
    }
})();
