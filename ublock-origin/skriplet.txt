ceor.js application/javascript
(() => {
    const selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") {
        return;
    }
    const click = () => {
        const elements = document.querySelectorAll(selector);
        for (const element of elements) {
            element.click();
        }
    };
    if (document.readyState === "interactive" ||
        document.readyState === "complete") {
        click();
    } else {
        addEventListener("DOMContentLoaded", click);
    }
})();

ceol.js application/javascript
(() => {
    const selector = "{{1}}";
    if (selector === "" || selector === "{{1}}") {
        return;
    }
    const click = () => {
        const elements = document.querySelectorAll(selector);
        for (const element of elements) {
            element.click();
        }
    };
    if (document.readyState === "complete") {
        click();
    } else {
        addEventListener("load", click);
    }
})();

esetco.js application/javascript
(() => {
    var data = "{{1}}";
    var path = "{{2}}";
    var domain = "{{3}}";
    var secure = "{{4}}";
    var del = "{{5}}";
    if (data.indexOf("=") === -1) return;
    if (del === "true") {
        data += ";max-age=-100";
    } else {
        data += ";max-age=2592000";
    }
    if (path !== "" && path !== "{{2}}") {
        data += ";path=" + path;
    }
    if (domain !== "" && domain !== "{{3}}") {
        data += ";domain=" + domain;
    }
    if (secure === "true") {
        data += ";secure";
    }
    document.cookie = data;
})();

setco.js application/javascript
(() => {
    const data = "{{1}}";
    if (data.indexOf("=") === -1) return;
    document.cookie = data;
})();

acls.js application/javascript
(() => {
    const token = "{{1}}";
    if (token === "" || token === "{{1}}") return;
    const tokens = token.split(/\s*\|\s*/);
    let selector = "{{2}}";
    if (selector === "" || selector === "{{2}}") return;
    const addclass = () => {
        try {
            const nodes = document.querySelectorAll(selector);
            for (const node of nodes) {
                node.classList.add(...tokens);
            }
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", addclass, { capture: true, once: true });
    } else {
        addclass();
    }
})();

attr.js application/javascript
(() => {
    const token = "{{1}}";
    if (token === "" || token === "{{1}}") return;
    const selector = token.split(/\s*\|\s*/).join(",");
    const name = "{{2}}";
    if (name === "" || name === "{{2}}") return;
    const value = "{{3}}";
    if (value === "" || value === "{{3}}") return;
    const addattr = () => {
        try {
            const nodes = document.querySelectorAll(selector);
            for (const node of nodes) {
                if (node.href.indexOf(location.hostname) === -1) {
                    node.setAttribute(name, value);
                }
            }
        } catch (ex) {}
    };
    if (document.readyState === "loading") {
        window.addEventListener("DOMContentLoaded", addattr, { capture: true, once: true });
    } else {
        addattr();
    }
})();

stw.js application/javascript
(() => {
    const path = "{{1}}";
    if (path === "" || path === "{{1}}") return;
    if (location.pathname.toLowerCase().indexOf(path) == -1) return;
    const selector = "{{2}}";
    if (selector === "" || selector === "{{2}}") return;
    const yaxis = "{{3}}";
    if (yaxis === "" || yaxis === "{{3}}") return;
    const scrolled = () => {
        const elem = document.querySelector(selector);
        if (elem) {
            elem.scrollIntoView();
        } else {
            window.scroll(0, yaxis);
        }
    };
    if (document.readyState === "interactive" ||
        document.readyState === "complete") {
        scrolled();
    } else {
        addEventListener("DOMContentLoaded", scrolled);
    }
})();
